-- Example only -- renamed .sql.example so dbt does not pick it up (scope
-- stops at Phase 9: "write no dbt models"). Rename to .sql once dwh
-- modeling begins. Shows the intended pattern: SCD2 history over a raw_b2b
-- dimension, keyed on the source's own updated_at (see README "Design
-- decisions" for why commission_rate etc. are snapshotted at sale time
-- rather than relying solely on this).
--
-- {% snapshot organizers_snapshot %}
--
-- {{
--     config(
--       target_schema='dwh',
--       unique_key='organizer_id',
--       strategy='timestamp',
--       updated_at='updated_at',
--     )
-- }}
--
-- SELECT * FROM {{ source('raw_b2b', 'organizers') }}
--
-- {% endsnapshot %}
